# Rime dictionary
# encoding: utf-8
# 
# rime配置的部署位置：
# ~/.local/share/fcitx5/rime 或者 ~/.config/ibus/rime  (Linux)
# ~/Library/Rime  (Mac OS)
# %APPDATA%\Rime  (Windows)
# 
#

---
name: wanxiang_t9
version: "LTS"
sort: by_weight  #字典初始排序，可選original或by_weight
use_preset_vocabulary: false
import_tables:
  # 1. 先完整引入 wanxiang.dict.yaml 中的 import_tables 列表
  __include: wanxiang.dict:/import_tables

  # 2. 将索引为 0 的第一项直接覆盖重写为新路径
  /[0]: dicts/zi_level1_3500
...